# Database
DATABASE_URL=postgresql://mission_control:mission_control_secret@localhost:5432/mission_control

# Server
PORT=3000
NODE_ENV=development

# Webhook (where to notify the bot)
WEBHOOK_URL=http://localhost:8080/api/webhooks/mission-control

# Web app (optional): API base URL for REST/SSE/WebSocket. Omit to use Vite proxy (same-origin).
# Local dev: VITE_API_URL=http://localhost:3001
# Network access (from another device): VITE_API_URL=http://<LAN_IP>:3001 (API and web listen on 0.0.0.0)
# VITE_API_URL=
